<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <application
        android:label="Anime Road Runner"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <!-- REQUIRED for flutter_scene / Flutter GPU (Impeller) on Android.
                 Without this flag Scene.initializeStaticResources() throws
                 "Failed to initialize ShaderLibrary" at startup, which happens
                 BEFORE the first Flutter frame is ever drawn, so the OS launch
                 screen never gets replaced and the app appears to hang
                 forever on the splash screen. macOS carries the matching
                 opt-in as FLTEnableFlutterGPU in Info.plist; this is the
                 Android equivalent. -->
            <meta-data
              android:name="io.flutter.embedding.android.EnableFlutterGPU"
              android:value="true"
              />
            <!-- Startup-hang diagnosis (see conversation history): forcing
                 OpenGLES did not fix the freeze on the reporter's device, so
                 this now forces Vulkan explicitly for the next test. If the
                 hang persists with BOTH backends forced, the backend is not
                 the cause. main.dart's boot screen shows a live elapsed-time
                 counter specifically so a true engine-level freeze (counter
                 stops) can be told apart from slow-but-progressing shader
                 compilation (counter keeps going) without a debugger. -->
            <meta-data
              android:name="io.flutter.embedding.android.ImpellerBackend"
              android:value="vulkan"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
